# DGGT pure-Python / platform-neutral dependencies for PPU torch 2.9.
#
# Install order:
#   1) install requirements-ppu-torch29-ppu-wheels.txt from the Alibaba PPU index
#   2) install this file from PyPI
#   3) compile local third_party/pointops2 if TAPIP3D tracking is needed
#
# Use ../install_ppu_torch29.sh from the repository root to run the full sequence.

# Keep NumPy 1.x. The original CUDA stack required this for binary
# compatibility with OpenCV/Open3D/custom extensions; keep the same policy on
# PPU because pointops2 and gsplat are native extensions.
numpy==1.26.4

av
IPython
open3d==0.19.0
lpips
scikit-image
transformers==4.57.6
accelerate==1.11.0
imageio-ffmpeg==0.5.1
diffusers==0.34.0
typed-argument-parser
hydra-core
omegaconf
einops
rich
tqdm
zstandard
scipy
h5py
kornia
peft==0.16.0
sophuspy==1.2.0

# Keep requests' dependency checker quiet and satisfy elastic-transport's
# urllib3<2 constraint from the Alibaba DSW base image.
requests==2.32.3
urllib3==1.26.20
chardet==5.2.0
charset-normalizer==3.3.2

# Pin OpenCV below 4.12 so pip does not force NumPy 2.x on Python 3.12.
# Use headless for server/PPU training. vLLM 0.17.1+cu130 may require
# opencv-python-headless>=4.13.0, but that pulls NumPy 2.x and is incompatible
# with this DGGT native-extension stack.
opencv-python-headless==4.11.0.86

rerun-sdk==0.23.1
matplotlib
imageio
trimesh
huggingface_hub
python-box[all]~=7.0
timm
wandb

# Match the exact GMuon implementation used by the NVIDIA environment.
gram-newton-schulz @ git+https://github.com/nanovisionx/gmuon.git@4863cc2446f09a54d937c579d4e775c125f5ab2e

# Common utility deps used by dataset / pointcloud tooling.
pandas
plyfile
pyquaternion
